# macOS
.DS_Store
._*

# Editor / OS
Thumbs.db
*.swp

# 學長姐海報原始素材夾（中文檔名）
# 跑過 tools/build-inspiration.py 之後，內容已轉成拼音複製進 inspiration/ 並提交，
# 原始夾保留在本機方便對照即可，不需要進 repo。若你的場景需要保留，移除以下兩行。
六年級網站整理素材/
inspiration-source/

# 私密名單與 build 產出
# private-data.local.json 含學生 email + 驗證碼,只在本機,由 tools/build-deploy.py 讀取
# _build/ 是注入後可直接貼到 Apps Script 的版本,同樣含個資
*.local.json
!*.example.json
_build/
